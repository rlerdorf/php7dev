---
ip: "192.168.7.7"
memory: 1024
cpus: 2

authorize:
    - ~/.ssh/id_rsa.pub
    - ~/.ssh/id_dsa.pub

keys:
    - ~/.ssh/id_rsa
    - ~/.ssh/id_dsa

networking:
    - public: true

folders:
    - map: www
      to: /var/www/default

sites:
#    - hostname: example-site
#      to: /home/vagrant/www/example-site

databases:
    - example_database
